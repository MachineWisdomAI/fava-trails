{
  "name": "rich_views",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "generate:fixture": "uv run python -m fava_trails.cli generate-reader --source ../tests/fixtures/fava_reader/data_repo --scope mwai/eng/fava-reader --output .",
    "generate:fixture:build": "uv run python -m fava_trails.cli generate-reader --source ../tests/fixtures/fava_reader/data_repo --scope mwai/eng/fava-reader --output . --build",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.4"
  }
}
