# Committed project scope for FAVA Trails.
# Agents read this to determine their trail_name on every tool call.
# Optional process override: export FAVA_TRAILS_SCOPE (do not write application .env files).
scope: mwai/eng/fava-trails
